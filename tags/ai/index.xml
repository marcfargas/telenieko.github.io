<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Ai on Marc Fargas</title>
    <link>https://www.marcfargas.com/tags/ai/</link>
    <description>Recent content in Ai on Marc Fargas</description>
    <generator>Hugo -- 0.155.3</generator>
    <language>en</language>
    <lastBuildDate>Sun, 15 Feb 2026 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://www.marcfargas.com/tags/ai/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Why I Built odoo-toolbox</title>
      <link>https://www.marcfargas.com/posts/why-i-built-odoo-toolbox/</link>
      <pubDate>Sun, 15 Feb 2026 00:00:00 +0000</pubDate>
      <guid>https://www.marcfargas.com/posts/why-i-built-odoo-toolbox/</guid>
      <description>I pointed Claude at Odoo&amp;#39;s docs. It hallucinated methods, invented field names, and wrote code that didn&amp;#39;t work. So I lent it a hand.</description>
      <content:encoded><![CDATA[<p>I run several companies on Odoo. Legal, accounting, property management — all of it flows through Odoo. Invoices, contracts, timesheets, CRM leads, accounting entries. Thousands of records, dozens of models, years of data.</p>
<p>When AI coding agents got good enough to write real code, the obvious move was: point the agent at Odoo and start asking questions. &ldquo;How much did we invoice last quarter?&rdquo; &ldquo;What&rsquo;s the average collection time for this client?&rdquo; &ldquo;Post a note on that CRM lead.&rdquo;</p>
<p>It didn&rsquo;t work.</p>
<h2 id="the-hallucination-problem">The hallucination problem</h2>
<p>Here&rsquo;s what happens when you ask Claude, GPT, or any frontier model to interact with Odoo&rsquo;s XML-RPC API:</p>
<p>It makes things up.</p>
<p>Not random garbage — plausible-sounding things. Method names that look right but don&rsquo;t exist. Field names that follow Odoo&rsquo;s naming conventions but aren&rsquo;t real. API patterns from older versions mixed with current ones. The model has absorbed enough Odoo knowledge from the internet to be dangerous, but not enough to be correct.</p>
<p>I&rsquo;d ask it to read invoices and it would call <code>account.invoice.search</code> — a model that was renamed to <code>account.move</code> years ago. I&rsquo;d ask for customer data and it would reference <code>customer_id</code> when the field is actually <code>partner_id</code>. It would construct domains using operators that don&rsquo;t exist in Odoo&rsquo;s filter syntax.</p>
<p>The output looked professional. It compiled. It even ran — right up until the Odoo server returned an error about a model or field that doesn&rsquo;t exist.</p>
<h2 id="why-the-obvious-solutions-dont-work">Why the obvious solutions don&rsquo;t work</h2>
<p><strong>&ldquo;Just give it the docs.&rdquo;</strong> Which docs? There&rsquo;s Odoo Community, Odoo Enterprise, and the OCA ecosystem — three overlapping codebases with different modules, different fields, sometimes different behavior for the same model. And that&rsquo;s before you account for the fact that every Odoo instance is different. Your instance has custom modules, specific patches, configuration flags that change field visibility, installed OCA addons that extend core models. The &ldquo;Odoo docs&rdquo; don&rsquo;t describe <em>your</em> Odoo — they describe a platonic ideal that doesn&rsquo;t match any production system. Even if you could figure out which docs to feed the model, dump all of it into context and you&rsquo;ve blown the window. Even with large contexts, the model drowns in irrelevant information about modules you don&rsquo;t use and patterns that don&rsquo;t apply.</p>
<p><strong>&ldquo;Use RAG.&rdquo;</strong> Retrieval-Augmented Generation sounds perfect in theory. Embed the docs, retrieve relevant chunks, feed them to the model. In practice, it retrieves the wrong patterns. A question about <code>account.move</code> retrieves fragments about invoicing, billing, credit notes, and journal entries — all mixed together, with no coherent mental model of how they relate. The model gets confused, not enlightened.</p>
<p><strong>&ldquo;Fine-tune on Odoo code.&rdquo;</strong> Ignoring the cost and maintenance burden, this still doesn&rsquo;t solve the core problem: Odoo&rsquo;s API has version-specific behavior, field-level quirks, and read/write asymmetries that only surface when you actually test against a running instance. A fine-tuned model confident in wrong patterns is worse than a base model that admits uncertainty.</p>
<p>The fundamental issue isn&rsquo;t lack of knowledge. It&rsquo;s lack of <em>structured, validated</em> knowledge.</p>
<h2 id="the-insight-progressive-tested-modules">The insight: progressive, tested modules</h2>
<p>The solution came from thinking about how a human Odoo developer learns. Nobody reads all 12,000 pages. You learn connection first, then how to query, then how to write, then domain-specific operations. Each layer builds on the previous one.</p>
<p>So I built that learning path — but for AI agents.</p>
<p><a href="https://github.com/marcfargas/odoo-toolbox">odoo-toolbox</a> is a set of knowledge modules that teach an agent Odoo progressively:</p>
<ol>
<li><strong>Connection</strong> — how to authenticate, manage sessions, handle the RPC protocol</li>
<li><strong>Domains</strong> — Odoo&rsquo;s query filter syntax, operators, composition rules</li>
<li><strong>Field types</strong> — the type system, the critical read/write asymmetries (you read a Many2one as <code>[id, name]</code> but write it as just <code>id</code>)</li>
<li><strong>CRUD</strong> — create, read, update, delete with all the edge cases</li>
<li><strong>Search</strong> — search patterns, pagination, field selection</li>
<li><strong>Introspection</strong> — discover models and fields dynamically (so the agent doesn&rsquo;t have to guess)</li>
<li><strong>Properties</strong> — user-defined dynamic fields, a common source of confusion</li>
<li><strong>Modules</strong> — module lifecycle, installation, dependency management</li>
</ol>
<p>Then domain-specific modules on top: mail and chatter, accounting entries, timesheets, attendance, contracts.</p>
<p>The agent loads only what it needs. Asking about invoices? Load connection + domains + CRUD + accounting. Posting a chatter message? Load connection + mail. The context stays focused.</p>
<h2 id="the-part-that-actually-matters-ci-testing">The part that actually matters: CI testing</h2>
<p>Here&rsquo;s the thing that separates this from yet another documentation project.</p>
<p>Every code example in every module is extracted and run against a real Odoo instance in CI. Not mocked. Not simulated. A Docker container with Odoo Community, a test database, and actual RPC calls.</p>
<p>The validation loop is simple and brutal:</p>
<ol>
<li>Write a skill module with code examples</li>
<li>CI extracts every code block</li>
<li>Each one runs against the real Odoo instance</li>
<li>If any example fails, the build fails</li>
</ol>
<p>This means the documentation can&rsquo;t rot. When Odoo&rsquo;s behavior changes, the CI catches it. When I write an example with the wrong field name, the CI catches it. When I describe an API pattern that doesn&rsquo;t actually work, the CI catches it.</p>
<p>125 commits over four weeks. Every one of them tested.</p>
<h2 id="the-other-part-that-actually-matters-introspection">The other part that actually matters: introspection</h2>
<p>The skills teach an agent how Odoo works <em>in general</em>. But your Odoo instance isn&rsquo;t general — it has custom models, OCA modules, configuration-specific fields, and behavior that no documentation covers.</p>
<p>That&rsquo;s where <a href="https://www.npmjs.com/package/@marcfargas/odoo-introspection"><code>@marcfargas/odoo-introspection</code></a> comes in. It lets the agent <em>ask your Odoo instance directly</em>: what models exist here? What fields does this model have? What type is this field, what&rsquo;s it related to, is it required?</p>
<p>The workflow looks like this:</p>
<ol>
<li>Agent connects to your Odoo instance using the connection skill</li>
<li>Agent uses the introspector to discover what&rsquo;s actually there — not what the docs say should be there, but what <em>is</em> there</li>
<li>Agent explores field types, relation targets, required fields, computed fields</li>
<li>Agent tests operations against the live instance</li>
<li>Once something works, it gets written into a skill module — a tested reference that won&rsquo;t hallucinate</li>
</ol>
<p>This is the loop that closes the gap between generic documentation and your specific Odoo. The base skills teach the agent <em>how</em> to talk to Odoo. The introspector teaches it <em>what your Odoo looks like</em>. And the CI pipeline ensures that what worked yesterday still works today.</p>
<h2 id="what-it-looks-like-in-practice">What it looks like in practice</h2>
<p>With odoo-toolbox loaded, an agent can answer questions it would otherwise hallucinate on:</p>
<p>&ldquo;What fields does <code>account.move</code> have for invoice amounts?&rdquo; — The agent uses the introspection module to discover fields dynamically, instead of guessing from training data.</p>
<p>&ldquo;Search for all draft invoices from January&rdquo; — The agent constructs a proper domain filter using the syntax it learned from the domains module, not an approximation from Stack Overflow fragments.</p>
<p>&ldquo;Post a note on partner record 42&rdquo; — The agent uses the mail/chatter module, which teaches the exact <code>message_post</code> method signature and its keyword arguments.</p>
<p>The difference isn&rsquo;t subtle. Before: the agent writes code that looks right and fails at runtime. After: the agent writes code that works, because it learned from tested examples, not internet fragments.</p>
<h2 id="the-ecosystem">The ecosystem</h2>
<p>The skills are one part. The toolbox also ships TypeScript packages that back them:</p>
<ul>
<li><strong><a href="https://www.npmjs.com/package/@marcfargas/odoo-client">@marcfargas/odoo-client</a></strong> — A lightweight RPC client. Handles authentication, session management, the JSON-RPC protocol. No Odoo framework dependency.</li>
<li><strong><a href="https://www.npmjs.com/package/@marcfargas/odoo-introspection">@marcfargas/odoo-introspection</a></strong> — Schema discovery. Ask &ldquo;what models exist?&rdquo; and &ldquo;what fields does this model have?&rdquo; programmatically.</li>
<li><strong><a href="https://www.npmjs.com/package/@marcfargas/create-odoo-skills">@marcfargas/create-odoo-skills</a></strong> — Scaffold your own skill project. Fork the structure, point it at your Odoo instance, test your own modules.</li>
</ul>
<p>The skills themselves are CC0 — public domain. Use them, modify them, ship them in your product, I don&rsquo;t care. The TypeScript code is LGPL-3.0.</p>
<p>Installing the skill is one command:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>npx skills add marcfargas/odoo-toolbox
</span></span></code></pre></div><h2 id="who-this-is-for">Who this is for</h2>
<p>Not everyone.</p>
<p>If you run a business on Odoo and work with AI coding agents — Claude Code, Cursor, pi, whatever — this gives your agent accurate, tested knowledge about how Odoo actually works. No hallucinations, no guessing, no &ldquo;let me try this method and see if it exists.&rdquo;</p>
<p>If you&rsquo;re building AI tooling for Odoo — chatbots, automation, reporting — the TypeScript packages give you a clean foundation without pulling in Odoo&rsquo;s entire framework.</p>
<p>If you don&rsquo;t use Odoo, this is irrelevant. And that&rsquo;s fine.</p>
<h2 id="whats-next">What&rsquo;s next</h2>
<p>This post is the origin story. The next two go deeper:</p>
<ul>
<li><strong>For business operators</strong>: How to get your AI agent working with your Odoo instance — the non-technical walkthrough, shown as an agent conversation. No code required.</li>
<li><strong>For developers</strong>: The progressive skill architecture — why it works, how to build your own skills, the testing pipeline that keeps everything honest.</li>
</ul>
<p>Both coming soon.</p>
<hr>
<p><em>odoo-toolbox is open source. <a href="https://github.com/marcfargas/odoo-toolbox">Repository</a> · <a href="https://www.npmjs.com/search?q=%40marcfargas%2Fodoo-">npm packages</a> · <a href="https://github.com/marcfargas/odoo-toolbox">Install the skill</a></em></p>
]]></content:encoded>
    </item>
    <item>
      <title>Building My AI Workbench</title>
      <link>https://www.marcfargas.com/posts/building-my-ai-workbench/</link>
      <pubDate>Sat, 14 Feb 2026 00:00:00 +0000</pubDate>
      <guid>https://www.marcfargas.com/posts/building-my-ai-workbench/</guid>
      <description>How a terminal coding agent became the backbone of everything I build — not by being smart, but by being composable.</description>
      <content:encoded><![CDATA[<p>Two weeks ago I had a collection of tools. A Hugo blog. An Odoo integration. Some CLI wrappers for Google APIs. A browser automation framework. They existed in separate repos, worked independently, and shared nothing.</p>
<p>Today they&rsquo;re an ecosystem. Not because I wrote some grand unification layer, but because I found a tool that thinks the way Unix thinks: everything is a file, every program does one thing, and <code>|</code> connects them.</p>
<p>This is the story of how <a href="https://github.com/badlogic/pi-mono">pi</a> — a minimal terminal coding agent — became my operating system for building things.</p>
<h2 id="what-pi-actually-is">What pi actually is</h2>
<p>Pi is not an IDE. It&rsquo;s not Cursor, not Copilot, not any of the AI-enhanced editors fighting for your attention. It&rsquo;s closer to bash: a harness that gives an LLM four tools (<code>read</code>, <code>write</code>, <code>edit</code>, <code>bash</code>) and gets out of the way.</p>
<p>The magic isn&rsquo;t in what pi ships with. It&rsquo;s in what pi <em>lets you add</em>:</p>
<ul>
<li><strong>Skills</strong> — markdown files that teach the model how to use specific tools. Like man pages, but for AI.</li>
<li><strong>Extensions</strong> — TypeScript modules that add new tool calls. Like adding <code>grep</code> to your PATH.</li>
<li><strong>Agents</strong> — named configurations with a persona, a model, and a set of skills. Like shell aliases with opinions.</li>
<li><strong>Chains</strong> — sequences of agents, where each step&rsquo;s output flows into the next. Like pipes.</li>
</ul>
<p>None of these concepts are revolutionary. They&rsquo;re borrowed from the oldest playbook in computing: small pieces, loosely joined.</p>
<h2 id="the-workbench-two-weeks-in">The workbench, two weeks in</h2>
<p>Here&rsquo;s what I&rsquo;ve built on top of pi in 14 days:</p>
<p><strong>18 skills</strong> across two layers. The <a href="https://github.com/marcfargas/skills">shared ones</a> are tool-specific and agent-agnostic — Azure CLI, Google Cloud, PM2 process management, web search, spreadsheet modeling, terminal recording, pre-release workflows. The personal ones handle my specific workflows — Bitwarden credential retrieval, browser launch sequences, document generation (Word, Excel, PowerPoint, PDF), multi-model design reviews.</p>
<p><strong>8 agents</strong> with different personalities. Four are reviewers that I run in parallel during design sprints — two architecture-focused, two implementation-focused. One is a synthesizer that merges their feedback. One is a scout that explores codebases. One delegates to OpenAI&rsquo;s Codex for pure coding tasks. One is a researcher.</p>
<p><strong>2 extensions</strong> that add persistent state. A TODO system that survives across sessions — the agent claims tasks, journals progress, marks them done. And a knowledge graph (&ldquo;brain&rdquo;) that extracts signals from emails, notes, and ERP records, clusters them into entities, and lets the agent resolve &ldquo;who is this phone number?&rdquo; against accumulated context.</p>
<p><strong>23 experiments</strong> — design documents that went through structured review cycles before any code was written. Each one followed the same pattern: capture the vision, run parallel reviewers across different models, synthesize, iterate.</p>
<p><strong>11 memoria entries</strong> — stable facts the agent records when it discovers something non-obvious. Unix sockets don&rsquo;t work on Windows (use named pipes). npm Trusted Publishing requires Node 24. PGLite date intervals produce timestamps, not dates. Each entry saves 2 hours of rediscovery.</p>
<h2 id="the-pattern-that-makes-it-work">The pattern that makes it work</h2>
<p>The Unix philosophy says: write programs that do one thing and do it well, write programs that work together, write programs that handle text streams.</p>
<p>Skills do one thing. The <code>odoo</code> skill teaches the model to query an Odoo ERP — field exploration, record operations, accounting queries. It knows nothing about email. The <code>go-easy</code> skill teaches the model Gmail, Drive, Calendar, and Tasks. It knows nothing about ERPs. The <code>sheet-model</code> skill teaches the model to build spreadsheets with live formulas. It knows nothing about where the data comes from.</p>
<p>But they compose. &ldquo;Read last month&rsquo;s invoices from Odoo, build a ratio model, email the CFO&rdquo; works — not because someone designed an invoice-to-spreadsheet-to-email pipeline, but because each skill handles its piece and the agent holds the thread.</p>
<p>That&rsquo;s the thing about composability: the interesting behaviors are the ones nobody planned for.</p>
<h2 id="beyond-code-the-business-design-sprint">Beyond code: the business design sprint</h2>
<p>Here&rsquo;s where it gets interesting. The same pattern that reviews software architecture also reviews business strategy.</p>
<p>I run a group of professional services companies — legal, accounting, property management. When we needed to prepare for a bank meeting with a client, I didn&rsquo;t just crunch numbers. I wrote a briefing document with the financial position, the adjustments we&rsquo;d planned, and what we needed from the bank. Then I ran four reviewers:</p>
<pre tabindex="0"><code>4 reviewers in parallel:
  - Fractional CFO         ×  Gemini 2.5 Pro
  - Bank relationship mgr  ×  Gemini 2.5 Pro
  - Fractional CFO         ×  Claude Sonnet
  - Bank relationship mgr  ×  Claude Sonnet
        │
        ▼
  Synthesizer merges all 4 reviews
</code></pre><p>The CFO personas dissected the client&rsquo;s EBITDA, found €45K–€80K of legitimate adjustments we hadn&rsquo;t considered — capitalizing internal development under Spanish GAAP, correcting accrual mismatches, recognizing unbilled revenue for Q4 work. The banking personas explained exactly how the bank&rsquo;s internal risk tool weights ratios, what the real thresholds are for SME credit in Spain, and why we should change the presentation order.</p>
<p>None of these reviewers are &ldquo;coding agents.&rdquo; They&rsquo;re business personas — a CFO who knows Spanish accounting, a bank analyst who knows how BBVA&rsquo;s risk engine works. The same chain infrastructure that runs architecture reviews runs financial strategy. The skill system doesn&rsquo;t care what domain the knowledge is in.</p>
<p>More recently, when a client was forced into a strategic decision — hire a replacement or double down on outsourcing — I wrote a vision document for transforming the business model. Four reviewers again, but this time: a fractional CFO, a COO with outsourcing experience, a strategy consultant, and a regulatory compliance specialist. The strategy reviewer&rsquo;s verdict was blunt: &ldquo;this isn&rsquo;t a strategic transformation, it&rsquo;s an operational optimization dressed up as one — where&rsquo;s your moat?&rdquo; The compliance reviewer mapped every regulatory risk across four professional service areas. The COO said the vision was missing the entire &ldquo;how&rdquo; of daily operations.</p>
<p>That feedback reshaped the plan. And it cost us an hour of writing the vision document, plus five minutes of compute time.</p>
<h2 id="the-design-sprint-for-code">The design sprint for code</h2>
<p>The same workflow applies to software. When I have an idea — say, a Google Chat gateway for the company&rsquo;s AI assistant — I write a vision document: problem, goal, architecture, constraints, risks, open questions. Then four reviewers, this time architecture and implementation focused, across two different models. A synthesizer merges their feedback.</p>
<p>23 experiments went through this process. Some survived. Some got killed by the reviews — which is exactly the point. Killing a bad idea at the vision stage costs 10 minutes. Killing it after implementation costs a week.</p>
<p>The key insight: the reviewers are just markdown files with a persona. Creating a CFO reviewer or a banking analyst takes 5 minutes. Creating a regulatory compliance specialist for Spanish professional services takes 10. The chain infrastructure — parallel execution, output aggregation, synthesis — is generic. The domain knowledge lives in the personas.</p>
<h2 id="what-i-actually-built-with-it">What I actually built with it</h2>
<p>This isn&rsquo;t theoretical. In two weeks, working evenings and weekends:</p>
<ul>
<li><a href="https://github.com/marcfargas/go-easy"><strong>go-easy</strong></a> — A unified Google API library for AI agents. Gmail, Drive, Calendar, Tasks. OAuth flows, two-phase auth, CLI tools. Published to npm.</li>
<li><a href="https://github.com/marcfargas/odoo-toolbox"><strong>odoo-toolbox</strong></a> — Odoo ERP integration for AI agents. CI runs against a real Odoo instance. Published to npm.</li>
<li><a href="https://github.com/marcfargas/holdpty"><strong>holdpty</strong></a> — Cross-platform detached terminal sessions. Named pipes on Windows, Unix sockets everywhere else. Published to npm.</li>
<li><strong>This website</strong> — Rebuilt from two legacy Hugo sites, merged content from two languages, deployed with GitHub Actions. The agent did the migration, I made design decisions.</li>
</ul>
<p>16 published npm packages, a rebuilt website, 23 design documents, and a knowledge graph. None of it written by the AI. All of it written <em>with</em> the AI — the agent handled the scaffolding, the CI/CD, the release mechanics, the tedious migrations, while I focused on what matters: what to build and why.</p>
<p>We event built a test harness for the agent so it can verify it&rsquo;s own modifications, <a href="https://github.com/marcfargas/pi-mf-extensions/tree/main/packages/pi-test-harness">pi-test-harness</a></p>
<h2 id="the-carpenters-workshop">The carpenter&rsquo;s workshop</h2>
<p>I keep coming back to this metaphor: a carpenter&rsquo;s workshop.</p>
<p>A good workshop isn&rsquo;t about having the fanciest tools. It&rsquo;s about having tools you know intimately, arranged the way you think, adapted to the work you do. The table saw is bolted where you need it. The jigs are purpose-built. The wood rack is sorted by project, not by species.</p>
<p>My pi setup is my workshop. The skills are my jigs — purpose-built for specific operations. The agents are my apprentices — each trained for a particular kind of work. The memoria is the notebook on the wall — hard-won lessons I don&rsquo;t want them to relearn. The experiments are my sketches — ideas tested cheaply before committing wood.</p>
<p>None of this is a product. It&rsquo;s not meant to be installed by anyone else. It&rsquo;s a personal workbench, built incrementally over two weeks, adapted to how I think and what I build.</p>
<p>And that&rsquo;s the point. The best tools are the ones you build for yourself.</p>
<hr>
<p><em>This post is part of the <a href="/series/ai-workbench/">AI Workbench</a> series, where I document the evolving setup. Next up: the Unix philosophy angle — why skills, not prompts, are the right abstraction for AI agents.</em></p>
]]></content:encoded>
    </item>
  </channel>
</rss>
